spring:
  application:
    name: demo
  jwt:
    secret: ${JWT_SECRET}
    accessTokenExpiration: 900 # 7d
    refreshTokenExpiration: 604800 # 7d
  profiles:
    active: dev
  # server:
  #   servlet:
  #     context-path: /myapp
stripe:
  secretKey: ${STRIPE_SECRET_KEY}
  webhookSecretKey: ${STRIPE_WEBHOOK_SECRET_KEY}
